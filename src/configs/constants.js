const ROLE_SHOP = {
  ADMIN: 'admin',
  USER: 'user',
  SHOP: 'shop',
  WRITER: 'writer',
  EDITOR: 'editor'
}

const STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
  PENDING: 'pending'
}

const HEADER = {
  API_KEY: 'x-api-key',
  AUTHORIZATION: 'authorization'
}

module.exports = {
  ROLE_SHOP,
  STATUS,
  HEADER
}
